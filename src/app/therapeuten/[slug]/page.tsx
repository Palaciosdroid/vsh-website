export default function TherapeutProfilPage({ params }: { params: Promise<{ slug: string }> }) {
  return (
    <div className="mx-auto max-w-4xl px-4 py-10 sm:px-6 lg:px-8">
      <p className="text-muted-foreground">
        Therapeuten-Profilseite wird in Phase 3 implementiert.
      </p>
    </div>
  );
}
